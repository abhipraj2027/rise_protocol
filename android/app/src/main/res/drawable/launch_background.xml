<?xml version="1.0" encoding="utf-8"?>
<!-- Plain near-black background matching the ringing screen's theme, so the
     brief native-splash frame doesn't flash white before Flutter attaches. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/black" />
</layer-list>
